[project]
name = "school-management-system-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dataretrieval>=1.1.3",
    "django>=6.0.4",
    "django-cors-headers>=4.9.0",
    "django-q2>=1.10.0",          # Async task queue
    "django-redis>=7.0.0",        # Redis cache backend
    "djangorestframework>=3.17.1",
    "hiredis>=3.4.0",             # Faster Redis parser
    "pillow>=12.2.0",
    "psycopg2-binary>=2.9.12",
    "python-dateutil>=2.9.0.post0",
    "python-decouple>=3.8",
    "redis>=8.1.0",             # Redis client
]

[dependency-groups]
dev = [
    "django-browser-reload>=1.21.0",
    "pre-commit>=4.6.1",
]
